{
  "name": "n8n-workflow-factory",
  "version": "0.1.0",
  "description": "n8n 工作流开发、管理、部署平台",
  "private": true,
  "scripts": {
"lint": "eslint . --no-config-lookup --no-error-on-unmatched-pattern",
    "test": "jest --passWithNoTests",
    "validate": "node scripts/validate-workflows.js",
    "deploy": "node scripts/deploy.js"
  },
  "keywords": [
    "n8n",
    "workflow",
    "automation"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^9.24.0",
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
